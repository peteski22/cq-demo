[project]
name = "craic-demo"
version = "0.1.0"
description = "Demo project for CRAIC knowledge commons"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
